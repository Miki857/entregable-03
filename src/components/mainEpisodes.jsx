import React from 'react'
import ReactDOM from 'react-dom/client'
import AppEpisodes from './AppEpisodes.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode>
    <AppEpisodes />
  // </React.StrictMode>,
)
